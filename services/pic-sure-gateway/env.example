# Copy to .env for local docker-compose runs (gitignored).
# URL the catch-all route forwards to (the legacy WildFly).
WILDFLY_URL=http://wildfly:8080
# Spring profile: aio (Docker DNS) or local.
SPRING_PROFILE=aio
