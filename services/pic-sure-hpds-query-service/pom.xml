<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>edu.harvard.hms.dbmi.avillach</groupId>
        <artifactId>pic-sure-api</artifactId>
        <version>${revision}</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>pic-sure-hpds-query-service</artifactId>
    <version>${revision}</version>
    <packaging>jar</packaging>

    <name>pic-sure-hpds-query-service</name>
    <description>The single HPDS ingress: executes queries + concept search against HPDS. DB-FREE -- persists/loads
        queries by calling pic-sure-operations-service's internal query API over HTTP instead of owning any database.
        Trusts the gateway's X-User-* headers; performs no JWT validation locally.</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!-- Registers /actuator/prometheus (PrometheusScrapeEndpoint), gated by ActuatorSecurityConfig. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- GatewayUser/GatewayUserResolver, PicsureException, GatewayExceptionAdvice, RequestIdFilter. -->
        <dependency>
            <groupId>edu.harvard.hms.dbmi.avillach</groupId>
            <artifactId>pic-sure-spring-commons</artifactId>
        </dependency>
        <!-- v1/v3 query models + QueryTranslator: the v3 Query record IS the public query ingress contract; the
             translator is used by /metadata to convert stored v1 bodies to v3. -->
        <dependency>
            <groupId>edu.harvard.hms.dbmi.avillach.hpds</groupId>
            <artifactId>pic-sure-hpds-model</artifactId>
        </dependency>
        <!-- Typed cross-service contracts: QueryStatusResponse, SearchRequest, PaginatedResponse, SignedUrlResponse. -->
        <dependency>
            <groupId>edu.harvard.hms.dbmi.avillach</groupId>
            <artifactId>pic-sure-contracts</artifactId>
        </dependency>

        <!-- Pooled RestClient backing (HPDS + operations-service HTTP clients). Version managed by the
             spring-boot-dependencies import in the BOM. -->
        <dependency>
            <groupId>org.apache.httpcomponents.client5</groupId>
            <artifactId>httpclient5</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub HMS-DBMI Apache Maven Packages</name>
            <url>https://maven.pkg.github.com/hms-dbmi/pic-sure</url>
        </repository>
    </distributionManagement>
</project>
