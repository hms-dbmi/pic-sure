package edu.harvard.hms.dbmi.avillach.conventions.persistencefixtures;

/**
 * Stands in for an enum compiled in another module, which the importer sees as an ordinary type.
 */
public class ForeignType {
}
